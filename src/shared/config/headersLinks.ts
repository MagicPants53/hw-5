import { paths } from './paths';

export const headersLinks = [
  { name: 'Products', path: paths.products },
  { name: 'Categories', path: paths.categories },
  { name: 'About us', path: paths.aboutUs },
];
